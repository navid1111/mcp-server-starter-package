# Library Utilities

This directory contains reusable utility functions and libraries used by the CLI and generators.

## Planned modules

- `fs.ts` - Filesystem utilities (idempotent writes, copy operations)
- `templates.ts` - Template engine and renderer
- Additional utilities as needed

These will be implemented in Phase 2 (Foundational).
